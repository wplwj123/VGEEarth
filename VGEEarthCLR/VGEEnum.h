#pragma once

namespace VGE{
	enum EnumAction3D{};

	enum EnumAniObjectType{};

	enum EnumAniKeyFramePosType{};

	enum EnumAniEffectType{};

	enum EnumAreaMeasureMode{};

	enum EnumDistanceMeasureMode{};

	enum EnumCameraMode{};

	enum EnumAltitudeMode{};

	enum EnumDataSourceType{};

	enum EnumGeoReferenceType{};

	enum EnumDatasetType{};

	enum EnumLayerType{};

	enum EnumRenderDetail{};

	enum EnumAlign{};

	enum EnumLightMode{};

	enum EnumLightType{};

	enum EnumStatusBarText{};

	enum EnumTrackMode{};

	enum EnumEditType{};

	enum EnumFlyRepeatValueType{};
}